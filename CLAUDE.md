# あたやさいさいゲームランド — 運用のきまり

公開先 → https://atayasaisai.github.io/game_land/
リポジトリ → https://github.com/atayasaisai/game_land （public / GitHub Pages は main の直下）

## この作り方を崩さない

- 素の HTML / CSS / JavaScript だけ。ビルド工程も npm も外部ライブラリも入れない。
- 1ゲーム＝1フォルダ、中に `index.html`。他のゲームに依存させない。
- あそんだ記録は `localStorage` だけ。サーバーには何も送らない。Cookie も使わない。
- 読み手は子ども。文章はやさしく、漢字は控えめに。

## 手もとで表示を確かめる

    py -m http.server 8399        （Windows）
    python3 -m http.server 8399   （macOS）

→ http://127.0.0.1:8399/ 　止めるのは Ctrl+C。

`index.html` をダブルクリックで直接開くと、相対パスの画像や問題パックが
読み込まれずに止まるブラウザがあります。かならずこのサーバ経由で見ること。

## 公開のしかた

    git add -A
    git commit -m "..."
    git push

数分で公開先に反映されます。ほかに手順はありません。

## ゲームを1本ふやす

1. フォルダを置く（中に `index.html`）
2. `.gitignore` にその名前があれば、その行を消す
3. `index.html` の、その科目の `<div class="grid">` の中に `<a class="tile" href="フォルダ名/">` を
   1枚足す（となりの札をコピーして、絵・名前・ひとこと・リンク先を変える）。
   札の絵は `img/mon_*.png`（各クエストのモンスターを小さくしたもの）。
   見出しの「○つ」の数も直す。科目そのものを足すときは、看板（`nav.subjects`）と段（`section.sec`）を1組足す。
4. `sitemap.xml` にも1行足す

## トップページのつくり（2026-09-24 に3代目へ作り直し）

- 上から：安心バッジの帯 → 題字（`img/title.jpg`）→ ひとこと → 科目の看板（押すと段へ飛ぶ・上に固定）→
  おすすめの段（えいごタイピング・げんしのへや・とどうふけん・百人一首・くく・ブロックくずし・にんじゃくん・6人の賢者 を再掲。札の左上に科目名）→ 科目ごとの段（さんすう／こくご／えいご／りか／しゃかい／おんがく／せいかつ）。「せいかつ」の段は 2026-09-26 に新設（色 `--liv`）。「おたのしみ」の段は 2026-09-25 にやめ、6人の賢者は「しゃかい」へ移した→ おうちのかたへ（3つの約束・寄付・LINEスタンプ）
- 背景は `img/bg.jpg`（題字と同じ絵の文字なし版）。
- JavaScript は `<head>` の小さな1つだけ（2026-09-25）。`…/#parents` のような印つきURLや読みこみなおしでも、いつも上から見せる。
  ページ内のリンク（看板・「くわしくは下へ」）はURLに `#` を残さずにその段へすべらせる。
- ガチャ・気分フィルタ・構想中ゲームの一覧は、3代目でやめた。
- 「国旗クエスト」「首都あてクエスト」は 2026-09-24 に `15_kokki_quest/`・`16_shuto_quest/` へ分けた（02 の写し＋パック1つ）。
  `02_nandemo_learning_quest/` は古いURLのために残してあるが、トップからは入らない。
- ゲームの最初の画面は、トップと同じ背景（各フォルダの `bg.jpg`、04 は `img/bg.jpg`）と、題字に似せた木の看板のロゴ（CSS）でそろえてある。
  06〜16 は同じ作り。04 は看板の下の中身だけ、もとの夜空の板にのせてある。02（旧URL用）だけは前のまま。
- 2代目（ガチャのあったもの）は `atayasaisai-gameland_v2_2026-09-24.html`（非公開）と git のタグ `top-v2` で戻せる。
- 題字の元絵は `00_incoming/あたやさいさいゲームランドv2.png`（上＝題字、下＝背景）。

## フォルダの見取り図

| 場所 | 中身 | 状態 |
|---|---|---|
| `index.html` | トップページ（科目ごとのゲーム一覧・JSON-LD・OGP） | 公開中 |
| `img/` | トップページの題字・背景・札の絵 | 公開中 |
| `googleed8e4df7967b8491.html` | Google Search Console の持ち主確認（2026-09-25 に確認ずみ）。**消すと確認が外れる** | 公開中 |
| `02_nandemo_learning_quest/` | なんでもラーニングクエスト | 公開中 |
| `04_sekai_no_recipe/` | せかいのレシピ ／ げんしのへや（118この原子。データは `elements.js`） | 公開中 |
| `15_kokki_quest/` `16_shuto_quest/` | 国旗クエスト・首都あてクエスト（02と同じエンジン。国連加盟国193か国。旗の絵は flag-icons（MIT）を各フォルダの `flags/` に入れている。`packs/flags.js` を直したら index.html の `?v=` を新しくする） | 公開中 |
| `17_kotoba_no_hashi/` | ことばの橋 English Typing Game（英文の穴うめタイピング。クエストとは別の仕組み。英文は index.html の `STAGES`。1つの島で10問（`PER`）。島ごとの背景は `STAGES` の `bg`（2026-09-26。`00_incoming/イングリッシュ背景.png`・`２.png` の8場面を切り出した `bg_*.jpg`、てんき と にがての島 は もとの `bg.jpg`）。最後の橋のむこうに出していた お城の絵は 2026-09-26 に先方の指示でやめた。最後は右はしまで走って「ゴール！」） | 公開中 |
| `18_hyakunin_isshu/` | 百人一首かるた（100首は `poems.js`。本文・読み・作者・決まり字はウィキペディア「小倉百人一首」と照合ずみ。意味の文は自作） | 公開中 |
| `19_todofuken_quest/` | とどうふけんクエスト（形は Natural Earth から自前変換。北方領土は北海道に入れ、東京は本土だけ・沖縄は本島だけ・長崎は対馬壱岐なし） | 公開中 |
| `20_bunshi_no_heya/` | せかいのレシピ ぶんしのへや（分子20こ。原子を えらぶだけで 手が自動でつながる（2026-09-25 に「つなぐ」操作をやめた）。分子ごとの できあがりの形は index.html の `SHAPE`（席の場所とつながり）で、えらんだ原子をその席にすわらせ、両はしがそろった つながりを引く。席のない原子は「いらない原子」として下にならび、タップではずす。見た目は 04 のCSSを写したもの） | 公開中 |
| `06`〜`14` の `*_quest/` | くく・たしざん・ひきざん・かけざん・わりざん・かんじ・えいたんご・げんそきごう・ねんごう（02と同じエンジン） | 公開中 |
| `21_rokunin_no_kenja/` | 6人の賢者 〜あたやさいさい国発展記〜（6人の賢者に6回たのむ順番で国が変わる。ルールは `rules.js`、絵の置き場は `art.js`、音はWebAudioで合成。**しらべ札（社会を学べる王国記）**（2026-09-25。9/26 に通常モードをやめて、このモード1つにした）：建物ができたり何かがおきたりした**あと**に、地図の左に「しらべ札」が出る（えらぶ前には出さない＝答えのヒントにしない）。札35まいは `shakai.js`（1回えらぶごとに2まいまで・まだ持っていない札を先に出す＝`CARDS_PER_PICK`。のこりは次の回へ。大震災の話は入れない）（地理・歴史・公民。数には出どころつき。数が古くなったらここを直す）、きっかけは index.html の `learnTriggers()`、地図の左の「社会の豆知識」の囲み（`#learnbox`）に出る、あつめた札は `kenja.note`（タイトルの「しゃかいノート」）。近代の建物・乗りもの（国会・裁判所・スーパー・デパート・モール・電波塔・空港＋飛行機・自動車・フェリー・ヘリ・天文台・アンテナ、100%でロケット。空を飛ぶもの（鳥・鷲・飛行機・ヘリ・ドラゴン）は建物より上の `L-sky` に描き、その場でゆらゆら（`.drift`）させるだけ）。人は人口÷35人（map.jpg の草地・砂地から拾った160か所 `GROUND` に島じゅうまばらに。建物の絵の上になる所は除く＝`crowd()`、建物より手前の `L-crowd`）、大きさは高さ36（何人かの絵は30）でそろえる。自動車は道の中心線を行き来（`cars()`、3台まで、建物のうしろを通る `L-cars`）。ロケットは発射台の絵から切り出した `rocket_body`・`rocket_flame` だけを飛ばし、台はロケットをぬいた `rocket_pad`。組み合わせの建物は1つずつ（`showCombo`）、人のすみかも1けんずつ（`growSites`）ふえる。はってん度とサブ指標は6回えらび終わるまで出さない（`#game.hidescore`）。絵を直したら art.js の `?v=` を上げる（SVGの画像は古いものが残りやすい）。悪化の絵は `badSigns()` が数を決める：4回目以降で はってん30%未満（worse）のときだけ、治安・食料・はってん度の悪さに比例して モンスター（国じゅうに最大16）・山賊・ドラゴン（最大3）・魔王の城・洪水（湖と川べり最大7）・噴火する山（最大6）がふえる。順調な道の序盤は巣のまわりに2ひきまで。46656通りを総当たりして、75%以上になる道では worse にならないことを確認。雨雲は下手なので削除（先方判断）は `00_incoming/発展記の素材２.jpg` から切り出した `obj3/`。建物は art.js の COMBOS の `site` で人のすみかを建てかえる（地図があいていないため）。戦艦・空母・潜水艦・鳥は使っていない。題字 `title.jpg`・地図 `map.jpg`・建物は先方の素材シート2枚を切り出した `obj/`（シート1）と `obj2/`（シート2、背景色との差でくり抜き）。番号の意味は `obj/sizes.js` のコメント。賢者5人は DYNATELEIA の絵 `sage/<色>/`（5ポーズでコマ送り）、いちの賢者は6人目が届くまで `chara/4.png` で仮置き、王さまは `chara/7.png`。動きの演出（鳥・船・動物・花火・虹・噴火・小さな人）はシート3・4から `fx/` に切り出し。元シートは `00_incoming/`） | 公開中（トップの「しゃかい」の段） |
| `22_ninja_run/` | 走れ！にんじゃくん！（ジャンプだけの横スクロール。エンドレスで、ずっと速くなりつづける（`speedAt`、上限なし）。忍者は画面のまんなかで走り、背景が流れる。障害物は「とびこえるもの」10種と、はこ・たる・石がき・丸太をロープでつるして下だけ空けた「とぶと失敗・そのまま走れば通れるもの」（`KINDS` の `up_*`。`up_long` は3〜5列つながった長いもの）。2ステージめからは、出る間隔のばらつきと、2つ組（ならび・かさね、`makeCombo`）がだんだん増える。同じ障害物は続けて出さない。背景と区別できるよう、背景 `stages.jpg` は少しぼかして淡くし、障害物 `obs.png` には細い光る白ふちを焼きこんである（忍者にはふちなし）。コマは `00_incoming/忍者くん.jpg`、障害物と題字 `title.png` は `忍者くんタイトル.jpg`（犬とライオンは左むきに反転）、背景は `忍者の背景.jpg` の4場面を草の線 y=310 にそろえたもの。トンネルの絵は不採用。**えいごアイテム モード**（2026-09-25）：画面の上に「おだい」（絵文字＋日本語）が出て、障害物のあいだに落ちている アルファベットを そのつづりの順に拾う（上はジャンプ・下は走って取る。ちがう文字でも ならびは消えない）。できると読み上げ＋意味＋効果。単語は小学校の3〜4文字50語（index.html の `WORDS`、300mまでは3文字だけ）＋とくべつな5語 STOP・SLOW・SAFE・SNOW・SOFT（`SPECIAL`、3つめのおだいから ときどき）。効果は `FX`：意味に合う語（RUN・JUMP・BIG・STAR・RAIN・SUN・MOON・HOT）はその効果、ほかは絵文字が空を飛ぶ＋小さなおまけ（`HELP`）。SLOW は はやさを最初（×1.0）にもどす（2026-09-26。ヘルプのおまけの SLOW も同じ）。はやくなるペースは、ふつうのモードの半分（`EIGO_ACC`。はやさは距離ではなく `sm` で決まる）。文字は間が1.3秒以上の所だけ（`placeLetter`）、このモードだけ障害物の間を+0.4秒。つづりは いつも うすく見えて次の文字が光る（`HINT_M`）。読み上げは端末内の英語の声だけ（`localService`）。**ハート**（2026-09-26）：えいごモードだけ ハート3つで始まり、ぶつかると1つへって その障害物はつぶれ、2秒むてき（`invT`、忍者がちかちか）。0でゲームオーバー。ヘルプのおまけに `LIFE`（ハート+1、5つまで）を足した（`HELP` は SAFE・SLOW・LIFE・JUMP・SOFT の順まわし）。ことば図鑑は `ninjaDash.words`、記録は `ninjaDash.bestEigo`。まちがえた文字を取ると「「Z」は ちがうよ！」と次に取る文字を大きく出すだけで、集めた文字は消さない（2026-09-26）。） | 公開中（トップの「えいご」の段） |
| `23_yojijukugo_karuta/` `24_kotowaza_karuta/` | 四字熟語かるた・ことわざかるた（18 百人一首の作りを写したもの。前半を読み、後半の札を取る。四字熟語50こ・ことわざ100こ（51〜100は後から足したので、番号順＝むずかしさ順ではない。組は index.html の `RANGES`）。データは各フォルダの `words.js`＝前半・後半・意味だけで、かなの読みと決まり字は index.html が自動で作る。前半が同じになるものは入れない・言い方がいくつかあるものはいちばんよく使われる形・意味の文は自作。2つの違いは index.html の `CFG` と題字・説明文だけ） | 公開中（トップの「こくご」の段） |
| `25_tsuki_no_michikake/` | 月のみちかけ（小4・小6理科。宇宙から見た図で月を動かすと、地球から見た形と、空のどこに見えるかが計算で出る。もとになる数は E＝太陽と月のはなれた角度 だけで、形・名前（`PH`）・空の位置（月は太陽より E/15 時間おくれて同じ道を通る）をすべてここから作る。空は みなみ向き・左が東。ステージ3つ（形あて・月を動かす・空の月）が主役で、じっけんしつは その下の「もっと よく わかりたい 人へ」に置く（2026-09-26。メニューは まりかと同じ 明るい札の作り＝「つきの ミッション」）。記録は `tsuki_michikake_rec`。絵はすべてSVGで自作） | 公開中（トップの「りか」の段） |
| `26_hoshizora_kurukuru/` | ほしぞら くるくる（小4「月と星」・中3「地球と宇宙」。北緯35°・東経135°の空を、日づけと時こくから計算して かく（canvas）。見る方角は 北・東・南・西・まうえ。空を指で左右にうごかすと時間が進む。星は index.html の `ST`（赤経・赤緯・等級・色。2000年の位置を手で入れた、星座の線に使う星と明るい星だけ）、星座は `CON`（線・名前の位置・季節・ひとこと）。ステージ3つ（北の空の回り方・東南西の星の動き・季節の星座）。記録は `hoshizora_kurukuru_rec`。惑星と月は出していない） | 公開中（トップの「りか」の段） |
| `27_classic_block/` | クラシックdeブロックくずし（トップの「おんがく」の段。ブロックの下に作曲家の肖像画があり、ぜんぶ くずすと 曲名と しょうかいが出る。**クリアするまで 曲名も 名前も 出さない**（一覧・画面とも「音楽家1」）。10ステージ＝ヴィヴァルディ〜ホルスト＋ボス「歓喜の歌」（1〜9クリアで出る。動く金のたてあり）。しょうかい文は index.html の `STAGES`。**ブロックの形は あそぶたびに `makeLevel()` が作る**（12列×16行。`MAKERS` の7しゅるい＝天井すき間・ろうか・とびら・しま・かたち・たからばこ・かいだん＋ボス用。どれも すき間や通路から ボールを 上に のせる しかけつき。こわれないブロック X は ほかを ぜんぶ くずすと きえる。下から とどかない ブロックは 自動で のぞく）。空きマスの絵は 黒い まくで かくし、くずした所だけ はがす。アイテム7種は `ITEMS`（くっつく＝ねらいうち）。20秒 当たらないと たすけアイテムが 落ちる。肖像画は Wikimedia Commons のパブリックドメインを `portrait/` に 600×570 で切り出し。曲は `songs.js`（自動生成。Mutopia の MIDI と Piano Letter Notes の音名譜から はじめの部分を取り出したもの）を WebAudio で合成。記録は `classic_block_rec`） | 公開中（トップの「おんがく」の段） |
| `28_mite_wakaru_bunsu/` | みてわかる ぶんすう（2026-09-26。小3〜6の分数を、テープと面積の図が動いて見せる。へや5つ＋クイズ：1 分数とは（ピザ・テープ、分母が大きいと1こが小さい。**つくってみよう**＝「2分の1を つくろう」の8問 `MAKE`、ピザやテープをタップしても ぬれる、「これで いく！」で判定。大きさが同じでも分母がちがえば「おしい」）・2 おなじ大きさ（分数のかべ）・3 たし算ひき算（ピースの大きさがちがう→1/L に切りなおす）・4 かけ算（1㎡の正方形の面積図）・**5 わり算（ひっくりかえす理由）**：最初に見るのは「はじめから」＝ピザを「1人 ○まいずつ くばる」話（`STORY`・`pizzaDiv`）で 6÷2 → 1÷2 → 1÷1/2 → 1÷1/3 → 2÷1/3 → 1÷2/3 → まとめ（先方の要望「そもそも割り算ってなんだっけ」から。2026-09-26）。くわしく①「いくつ入る？」＝ 1/d ではかる（×d）→ c こずつまとめる（÷c）→ 答え → 1/L に切ってたしかめ（`drawHakaru`）、見方2「1あたり」＝ c/d dL で a/b ㎡ → 1/d dL で1だん（÷c）→ 1dL で d だん（×d）（`drawAtaari`、教科書のペンキの問題の形）。クイズ10問は `GEN`（上どうし下どうしをたす・ひっくりかえさない、などのよくあるまちがいを選択肢に入れる）。まちがえると図で説明。記録は `bunsu_mieru_rec`。絵はすべてSVGで自作、札の絵は `img/bunsu.svg`） | 公開中（トップの「さんすう」の段） |
| `29_nandoku_kanji/` | なんどく漢字 よめるかな？（2026-09-26。難読漢字を4つの読みから えらぶ。10の組・253こは `kanji.js`（[漢字, よみ, 絵文字, ひとこと]）。読みが2つ以上よく使われるもの（紅葉・銀杏など）は入れない。まちがいの3つは同じ組の ほかの よみから自動。1回10問でモンスターのHPをけずる。💡ヒント＝はじめの1文字。読めた字は「ずかん」（`nandoku_got`）、まちがえた字は「にがて」の組（`nandoku_weak`）。字は端末の明朝（Webフォントに難しい字がないため）。見た目は 24 の写し） | 公開中（トップの「こくご」の段） |
| `30_bushu_quiz/` | ぶしゅクイズ この漢字の部首は？（2026-09-26。漢字1字を見せて、部首の名前を**入力して答える**クイズ。選択肢だと やさしすぎるため（先方の要望）。はじめは「タイピング練習」と取りちがえて作り、同じ日に作りかえた。部首74こは `bushu.js`（[形, 名前（べつの呼び方は / で）, 例の漢字, ひとこと]。例の漢字は漢和辞典の部首どおりのものだけ。問題の漢字は例から1つ）。組は位置ごと（へん①〜③・つくり・かんむり・あし・たれ・にょう・かまえ）。組の中で位置がちがう たれ・かまえ は index.html の `TARE`・`KAMAE`。こたえは ひらがな・カタカナ・ローマ字どれでも（ローマ字は `units()`／`step()`／`romaMatch()`。si/shi・kka/xtu・ん＝n/nn などをみな受けつける）。日本語入力で字をきめる Enter は答えにしない。まちがえるとヒントが1つずつ（①場所 ②形となかまの字 ③はじめの字）、3回でこたえ。ほかの部首の名前を答えると「きへん は 木 の 部首だよ」と出る。点はヒントなし3・1つ2・2つ以上1。「やさしい」は①②がはじめから出て2点まで。記録は `bushu_best`・`bushu_weak`） | 公開中（トップの「こくご」の段） |
| `31_kaiten_sushi/` | 写真でわかるかな？ 回転寿司クイズ（2026-09-26。はじめの名前は「さかなの かいてんずし」で、同じ日に先方の題字の絵に合わせて改名。回転ずしのレーンを 魚・貝の写真が 右から流れてきて、**目の前（レーンのまんなか）で止まり、10秒のカウントダウン**（写真の右上の まるい時計、のこり3秒で赤くなって音）。そのあいだに 4つから 名前を えらぶ。答えないと そのまま左へ ながれていく（2026-09-26 先方の案。前は止まらずに流れきるまでが時間だった）。動きは `run()` の `G.phase`（in→stop→out）、待ち時間は `COURSE` の `wait`。まちがえた おさらは その場に のこして 見くらべられるようにしてある。あたると その おさらが 手もとに つまれる。おさらの色＝むずかしさ（青100円・赤200円・金500円。2026-09-26 に先方の素材に合わせて 白→青）。「なみ」＝青と赤、「とくじょう」＝金も4さら、どちらも10さら・10秒。まちがいの3つは なるべく同じ なかま（さかな／貝／そのほか）から。73しゅるいは `sakana.js`（名前・漢字・皿・なかま・ひとこと（自作）・写真のクレジット。2026-09-26 に先方の要望で 43→73。**食べられる魚介だけ**）。写真は `photo/`：ウィキメディア・コモンズの PD／CC0／CC BY／CC BY-SA を 800×600 にそろえたもの（4:3 に近いものは切りぬき、ちがう形は ぼかした同じ写真を背景に。2026-09-26 に「写真を大きく・鮮明に」で 480×360 から作り直し、もとが小さかった14しゅるいは写真ごと差しかえ）。候補さがしは `00_incoming/回転寿司_写真さがし.py`、作るのは `回転寿司_写真づくり.py`（どちらも非公開）。写真を直したら index.html の `photo/…jpg?v=` と `sakana.js?v=` を上げる。写真はレーンの上で 画面はば88%（最大430px）まで大きく出す（`.lane` の `--W`）。**ファイル名は番号（`sakana.js` の `p`）で、答えがわからないようにばらばら**。タイトル画面や説明文にも 魚の名前・なかまを書かない（ネタバレ禁止＝先方の指示）。クレジットは ずかんの下に全部出す。**生きている／まるごとの姿の写真にする**（先方の判断。すしネタの写真ではない）。アナゴは よい写真がなく 入れていない。記録は `kaiten_sushi_rec`（ずかん＝あてた魚）。札の絵は `img/kaiten_sushi.svg`。**見た目**（2026-09-26 先方の指示「まりかのように インパクトを」）：先方の素材3枚 `00_incoming/回転寿司の素材１.png`・`素材２`・`素材３` から切り出した `art/`（題字の絵 `title.jpg`＝素材３の上半分・板前さん3つの顔 `chef`/`chef_ok`/`chef_ng`・おさら `plate_b/r/y`・レーンのベルト `belt.jpg`（つなぎめをまぜて くり返せる1まい）・おすしの のった おさら `sushi_*`・のれん・お茶・しょうゆ など）。切り出しのスクリプトは `00_incoming/回転寿司_切り出し.py`（非公開）。レーンに いっしょに のる おすしは、えらぶ4つに 入っている さかなのものは のせない（`FOOD`、ヒントにしない）） | 公開中（トップの「せいかつ」の段） |
| `32_jikken_tantei_marika/` | 実験探偵まりか（2026-09-26。名前は ひらがな（先方指定）。小3〜6理科の実験で なぞを とく推理ゲーム。じけんファイル9つ（`CASES`：お題・手もとの道具 `tools`・しらべるもの `sus`・決め手 `key`・答え `ans`・解説 `exp`）。道具9つは `TOOLS`、しらべるものは `OBJ`（でんぷん・電気・じしゃく・酸性/アルカリ性・とけている固体・二酸化炭素・もやすはたらき などの性質を持つ）で、結果の文と絵は `result()`／`scene()` が性質から作る。遊び方：①道具をえらぶ（えらぶまで②はうすく押せない）→②ものをタップすると「じっけん けっか」のまどが出る（そこからも「これだ？」をつけられる）→「これが答えだ！」。画面は明るい理科室の色（2026-09-26 先方の指摘「背景が暗い」「道具の選択が分かりにくい」で作り直し。タイトルの板も 同日「地味」との指摘で、明るい「じけんファイル」の札（CASE 01〜・色つき番号・かいけつのハンコ・かいけつ数）に作り直した）。えらんでいる道具が そのまま「決め手」になり、決め手と答えの両方が合えば正解。決め手の道具で1つも しらべていないと「証拠がない」。まちがい0回★3・1回★2・2回以上★1。記録は `jikken_tantei_rec`。絵は先方の素材シート2枚 `00_incoming/実験探偵まりか.png`（白地＋色の丸）・`実験探偵まりか２.png`（透過）から切り出した `art/*.png`（題字・まりかの顔3つ・道具・しらべるもの・結果の絵）。ヨウ素液や石灰水の色は、絵の上に SVG の色を かさねて出す（`drawObj()`、ヨウ素液をたらす場所は `SPOT`）。切り出しのスクリプトは `00_incoming/実験探偵まりか_切り出し.py`（非公開）。試験管1本の絵だけまだ無く、じけん9の試験管は自作のSVG。札の絵は `img/marika.png`） | 公開中（トップの「りか」の段） |
| `01_kingdom_of_six/` | 王国発展記 〜六つの選択〜 | 非公開（`.gitignore`） |
| `03_okane_oukoku/` | おかね王国のつくりかた | 非公開（`.gitignore`） |
| `05_kiri_no_sekiban/` | ルーンの谷と霧の王国 〜石板の旅人〜 | 非公開（`.gitignore`） |
| `00_incoming/` | 外から受け取った素材の元ファイル | 非公開（`.gitignore`） |

**非公開の4つは GitHub に入っていません。この端末にしか無いので、消さないこと。**
`02_nandemo_learning_quest/dist/` は配布用の書き出しで、同じフォルダの
`build-single.py` でいつでも作り直せます。

## 制作環境の事情（2026年9月にMacからWindowsへ移りました）

Claude の記憶（`~/.claude/projects/.../memory/`）には Mac 時代の作業手順が
書かれています。次の2つは **Windows には存在しません**。読み替えてください。

- `osascript -l JavaScript`（JXA）でJSを先に流す → 使えない
- `qlmanage -t` でHTMLをPNGにして目視 → 使えない。ブラウザで直接見ること
- `python3` → `py`

ブラウザペインでスクリーンショットを撮るとき、ペインが `hidden` だと
CSSアニメの0%フレームしか映らず、直前のフレームが残ることがあります。
動いていないように見えても実際は正常なことがあるので、
1秒待ってから撮り直して確かめること（記憶の `browser-pane-animation-trap` 参照）。

## 非公開の3本について

`03_okane_oukoku` は通しで遊べますが、**クリア条件が未調整**です（同フォルダの `STATUS.md`）。
`05_kiri_no_sekiban` は**序章＋第一話だけの試作品**で、`img/` の絵9枚は作り直せません。
どちらもトップページの一覧には載せていません。

## Google 検索への登録（2026-09-25）

- Google Search Console に「URL プレフィックス」で `https://atayasaisai.github.io/game_land/` を登録した。
  `github.io` は GitHub のドメインなので「ドメイン」の登録は使えない。
- 持ち主の確認は HTML ファイル方式で、`googleed8e4df7967b8491.html` を置いてある（中身は Google が決めた1行）。消すと確認が外れる。
- `sitemap.xml` を送信ずみ。送信した直後の表示は「取得できませんでした」だった（公開先のファイルは正常に開けて、20件）。
  何日たっても赤いままなら、行を開いて送信先に `game_land/` が入っているか見る。入っていなければフルの URL で送りなおす。
- トップの URL は「インデックス登録をリクエスト」ずみ。
- ゲームを足したら `sitemap.xml` に1行足すだけでよい。Google が自分で読みに来る。
- `#` から後ろは同じページの中の位置を表す印で、Google は別のページとしては登録しない。

## 積み残し

- Search Console のサイトマップが「成功しました」・ページ数 20 になったかを、数日後に確かめる。
- 訪問者カウント（GoatCounter）が3ページとも未設定。
  `index.html` / `02_nandemo_learning_quest/index.html` / `04_sekai_no_recipe/index.html`
  の末尾にコメントアウトされた1行があり、goatcounter.com で取ったコードを
  `MYCODE` の場所に入れてコメントを外すと動きます。
- `robots.txt` は置いていません。プロジェクトページ配下のものはクローラに読まれないため。
